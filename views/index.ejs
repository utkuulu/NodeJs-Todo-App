<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo APP</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
</head>
<body>
    <section class="vh-100" style="background-color: #eee;">
        <div class="container py-5 h-100">
          <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col col-lg-9 col-xl-7">
              <div class="card rounded-3">
                <div class="card-body p-4">
      
                  <h4 class="text-center my-3 pb-3">To Do App</h4>
      
                  <form method="POST" action"/" class="row row-cols-lg-auto g-3 justify-content-center align-items-center mb-4 pb-2">
                    <div class="col-12">
                      <div class="form-outline">
                        <input type="text" id="title" name="title" class="form-control" placeholder="Enter a Task Here" />
                        
                      </div>
                    </div>
      
                    <div class="col-12">
                      <button type="submit" class="btn btn-warning text-white" >Save</button>
                    </div>
                  </form>

                    <table class="table mb-4">
                      <thead>
                        <tr>
                          <th scope="col">No.</th>
                          <th scope="col">Todo item</th>
                          <th scope="col">Status</th>
                          <th scope="col">Actions</th>
                        </tr>
                      </thead>
                      <tbody>
  
                        <% for (let i = 0; i < allTasks.length; i++) { %>
                          
                        <tr>
                          <th scope="row"><%= i+1 %></th>
                          <td><%= allTasks[i].title %></td>
                          <td><%= allTasks[i].status %></td>
                          <td>

                          
                            <div class="container d-flex">
                              <form action="/delete/<%= allTasks[i]._id %>" method="POST">
                                <button type="submit" class="btn btn-danger">Delete</button>
                              </form>
                              
                              <form action="/start/<%= allTasks[i]._id %>" method="POST">
                                <button type="submit" class="btn btn-success ms-1">Started</button>
                              </form>
                              
                              <form action="/finish/<%= allTasks[i]._id %>" method="POST">
                                <button type="submit" class="btn btn-success ms-1">Finished</button>
                              </form>
                            </div>
                            
                                                        
                          </td>
                        </tr>
  
                        <% } %>
  
                      </tbody>
                    </table>

      
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  
</body>
</html>